# ores-otel-api-server.rs

SeaORM JSON API. HTTP is default; TCP and NATS are opt-in features in `src/transport`.
Env keys are generated by `f2e generate` from `.cli-flags.toml` into `generated/{rust,dart,typescript,gleam}`.
